import React from 'react';

const Filtrar = () => {
    return (
        <div>
            <h2>Filtrar</h2>
            {/* Contenido del componente Filtrar */}
        </div>
    );
};

export default Filtrar;
